import "package:flutter/material.dart";

class DigestiveListScreen extends StatelessWidget {
  const DigestiveListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Digestives"),
      ),
      body: Placeholder(),
    );
  }
}
